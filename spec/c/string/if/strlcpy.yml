SPDX-License-Identifier: CC-BY-SA-4.0 OR BSD-2-Clause
brief: |
  Copy the string referenced by ${.:/params[1]/name} to the string referenced by
  ${.:/params[0]/name} bounded by the size in bytes specified by
  ${.:/params[2]/name}.
copyrights:
- Copyright (C) 2026 embedded brains GmbH & Co. KG
definition:
  default:
    attributes: null
    body: null
    params:
    - char *restrict ${.:/params[0]/name}
    - const char *restrict ${.:/params[1]/name}
    - ${/c/if/size_t:/name} ${.:/params[2]/name}
    return: ${/c/if/size_t:/name}
  variants: []
description: null
enabled-by: true
index-entries: []
interface-type: function
links:
- role: interface-placement
  uid: header
- role: interface-ingroup
  uid: group
name: strlcpy
notes: |
  See [FreeBSD Manual Pages, strlcpy()](https://man.freebsd.org/cgi/man.cgi?query=strlcpy&sektion=3).
params:
- description: |
    is the pointer to the destination string.
  dir: out
  name: dst
- description: |
    is the pointer to the source string.
  dir: in
  name: src
- description: |
    is the size of the destination buffer.
  dir: null
  name: size
return:
  return: |
    Returns the length of the string referenced by ${.:/params[1]/name}. A return
    value greater than or equal to the size specified by ${.:/params[2]/name}
    indicates that the string referenced by ${.:/params[1]/name} has been truncated
    while copying it to the memory area referenced by ${.:/params[0]/name}.
  return-values: []
type: interface
