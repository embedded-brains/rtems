SPDX-License-Identifier: CC-BY-SA-4.0 OR BSD-2-Clause
copyrights:
- Copyright (C) 2021, 2025 embedded brains GmbH & Co. KG
enabled-by: true
links: []
test-actions:
- action-brief: |
    Create a mutex and validate the mutex directives.
  action-code: |
    const struct timespec invalid_abstime = {
      .tv_sec = -1,
      .tv_nsec = -1
    };
    int                   eno;
    struct _Mutex_Control mutex;

    _Mutex_Initialize( &mutex );

    ctx->tq_mtx_ctx.base.thread_queue_object = &mutex;
    ctx->tq_mtx_ctx.base.enqueue = MutexEnqueue;
    ctx->tq_mtx_ctx.base.surrender = MutexSurrender;
    ctx->tq_mtx_ctx.protocol = TQ_MTX_PRIORITY_INHERIT;
    ctx->tq_mtx_ctx.owner_check = TQ_MTX_NO_OWNER_CHECK;
    ctx->tq_mtx_ctx.priority_ceiling = PRIO_INVALID;
  checks:
  - brief: |
      Validate the ${../if/sys-lock-mutex-try-acquire:/name} directive.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_NO_WAIT;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_UNAVAILABLE;
      ${/score/mtx/req/seize-try:/test-run}( &ctx->tq_mtx_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-try-acquire
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-busy
  - brief: |
      Validate the ${../if/sys-lock-mutex-acquire-timed:/name} directive for
      valid timeout parameters.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_WAIT_TIMED;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_DEADLOCK;
      ${/score/mtx/req/seize-wait:/test-run}( &ctx->tq_mtx_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-acquire-timed
    - role: validation
      uid: ../req/sys-lock-mutex-clock-realtime
    - role: validation
      uid: ../req/sys-lock-mutex-omip
    - role: validation
      uid: ../req/sys-lock-mutex-priority-inherit
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-fatal
  - brief: |
      Validate the ${../if/sys-lock-mutex-acquire-timed:/name} directive for
      an invalid timeout parameter.
    code: |
      TQSetScheduler(
        &ctx->tq_mtx_ctx.base,
        TQ_HELPER_A,
        SCHEDULER_A_ID,
        PRIO_HIGH
      );
      TQSend( &ctx->tq_mtx_ctx.base, TQ_HELPER_A, TQ_EVENT_ENQUEUE );
      eno = _Mutex_Acquire_timed( &mutex, &invalid_abstime );
      T_eq_int( eno, EINVAL );
      TQSend( &ctx->tq_mtx_ctx.base, TQ_HELPER_A, TQ_EVENT_SURRENDER );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-acquire-timed
    - role: validation
      uid: ../req/sys-lock-mutex-clock-realtime
    - role: validation
      uid: ../req/sys-lock-mutex-invalid-timeout
  - brief: |
      Validate the ${../if/sys-lock-mutex-acquire-timed:/name} timeout
      behaviour.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_WAIT_FOREVER;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_DEADLOCK;
      ${/score/tq/req/timeout-priority-inherit:/test-run}(
        &ctx->tq_mtx_ctx.base
      );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-acquire-timed
    - role: validation
      uid: ../req/sys-lock-mutex-clock-realtime
    - role: validation
      uid: ../req/sys-lock-mutex-omip
    - role: validation
      uid: ../req/sys-lock-mutex-priority-inherit
  - brief: |
      Validate the ${../if/sys-lock-mutex-acquire:/name} directive.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_WAIT_FOREVER;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_DEADLOCK;
      ${/score/mtx/req/seize-wait:/test-run}( &ctx->tq_mtx_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-acquire
    - role: validation
      uid: ../req/sys-lock-mutex-omip
    - role: validation
      uid: ../req/sys-lock-mutex-priority-inherit
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-fatal
  - brief: |
      Validate the ${../if/sys-lock-mutex-release:/name} directive.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_WAIT_FOREVER;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_DEADLOCK;
      ${/score/mtx/req/surrender:/test-run}( &ctx->tq_mtx_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-release
    - role: validation
      uid: ../req/sys-lock-mutex-omip
    - role: validation
      uid: ../req/sys-lock-mutex-priority-inherit
  - brief: |
      Destroy the mutex.
    code: |
      _Mutex_Destroy( &mutex );
    links: []
  links: []
- action-brief: |
    Create a recursive mutex and validate the mutex directives.
  action-code: |
    const struct timespec invalid_abstime = {
      .tv_sec = -1,
      .tv_nsec = -1
    };
    int                             eno;
    struct _Mutex_recursive_Control mutex;

    _Mutex_recursive_Initialize( &mutex );

    ctx->tq_mtx_ctx.base.thread_queue_object = &mutex;
    ctx->tq_mtx_ctx.base.enqueue = RecursiveEnqueue;
    ctx->tq_mtx_ctx.base.surrender = RecursiveSurrender;
    ctx->tq_mtx_ctx.protocol = TQ_MTX_PRIORITY_INHERIT;
    ctx->tq_mtx_ctx.owner_check = TQ_MTX_NO_OWNER_CHECK;
    ctx->tq_mtx_ctx.priority_ceiling = PRIO_INVALID;
  checks:
  - brief: |
      Validate the ${../if/sys-lock-mutex-recursive-try-acquire:/name}
      directive.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_NO_WAIT;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_ALLOWED;
      ${/score/mtx/req/seize-try:/test-run}( &ctx->tq_mtx_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-try-acquire
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-allowed
  - brief: |
      Validate the ${../if/sys-lock-mutex-recursive-acquire-timed:/name}
      directive for valid timeout parameters.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_WAIT_TIMED;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_ALLOWED;
      ${/score/mtx/req/seize-wait:/test-run}( &ctx->tq_mtx_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-acquire-timed
    - role: validation
      uid: ../req/sys-lock-mutex-clock-realtime
    - role: validation
      uid: ../req/sys-lock-mutex-omip
    - role: validation
      uid: ../req/sys-lock-mutex-priority-inherit
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-allowed
  - brief: |
      Validate the ${../if/sys-lock-mutex-recursive-acquire-timed:/name}
      directive for an invalid timeout parameter.
    code: |
      TQSetScheduler(
        &ctx->tq_mtx_ctx.base,
        TQ_HELPER_A,
        SCHEDULER_A_ID,
        PRIO_HIGH
      );
      TQSend( &ctx->tq_mtx_ctx.base, TQ_HELPER_A, TQ_EVENT_ENQUEUE );
      eno = _Mutex_recursive_Acquire_timed( &mutex, &invalid_abstime );
      T_eq_int( eno, EINVAL );
      TQSend( &ctx->tq_mtx_ctx.base, TQ_HELPER_A, TQ_EVENT_SURRENDER );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-acquire-timed
    - role: validation
      uid: ../req/sys-lock-mutex-clock-realtime
    - role: validation
      uid: ../req/sys-lock-mutex-invalid-timeout
  - brief: |
      Validate the ${../if/sys-lock-mutex-recursive-acquire-timed:/name}
      timeout behaviour.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_WAIT_FOREVER;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_ALLOWED;
      ${/score/tq/req/timeout-priority-inherit:/test-run}(
        &ctx->tq_mtx_ctx.base
      );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-acquire-timed
    - role: validation
      uid: ../req/sys-lock-mutex-clock-realtime
    - role: validation
      uid: ../req/sys-lock-mutex-omip
    - role: validation
      uid: ../req/sys-lock-mutex-priority-inherit
  - brief: |
      Validate the ${../if/sys-lock-mutex-recursive-acquire:/name} directive.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_WAIT_FOREVER;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_ALLOWED;
      ${/score/mtx/req/seize-wait:/test-run}( &ctx->tq_mtx_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-acquire
    - role: validation
      uid: ../req/sys-lock-mutex-omip
    - role: validation
      uid: ../req/sys-lock-mutex-priority-inherit
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-allowed
  - brief: |
      Validate the ${../if/sys-lock-mutex-recursive-release:/name} directive.
    code: |
      ctx->tq_mtx_ctx.base.wait = TQ_WAIT_FOREVER;
      ctx->tq_mtx_ctx.recursive = TQ_MTX_RECURSIVE_ALLOWED;
      ${/score/mtx/req/surrender:/test-run}( &ctx->tq_mtx_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-mutex-recursive-release
    - role: validation
      uid: ../req/sys-lock-mutex-omip
    - role: validation
      uid: ../req/sys-lock-mutex-priority-inherit
  - brief: |
      Destroy the mutex.
    code: |
      _Mutex_recursive_Destroy( &mutex );
    links: []
  links: []
- action-brief: |
    Create a semaphore and validate the semaphore directives.
  action-code: |
    struct _Semaphore_Control sem;

    _Semaphore_Initialize( &sem, 0 );

    ctx->tq_sem_ctx.base.thread_queue_object = &sem;
    ctx->tq_sem_ctx.base.enqueue = SemaphoreEnqueue;
    ctx->tq_sem_ctx.get_count = SemaphoreGetCount;
    ctx->tq_sem_ctx.set_count = SemaphoreSetCount;
  checks:
  - brief: |
      Validate the ${../if/sys-lock-semaphore-try-wait:/name} directive.
    code: |
      ctx->tq_sem_ctx.base.surrender = SemaphorePostBinary;
      ctx->tq_sem_ctx.base.wait = TQ_NO_WAIT;
      ctx->tq_sem_ctx.variant = TQ_SEM_BINARY;
      SemaphoreSetCount( &ctx->tq_sem_ctx, 1 );
      ${/score/sem/req/seize-try:/test-run}( &ctx->tq_sem_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-semaphore-try-wait
  - brief: |
      Validate the ${../if/sys-lock-semaphore-wait-timed-ticks:/name} directive
      for valid timeout parameters.
    code: |
      ctx->tq_sem_ctx.base.surrender = SemaphorePostBinary;
      ctx->tq_sem_ctx.base.wait = TQ_WAIT_TIMED;
      ctx->tq_sem_ctx.variant = TQ_SEM_BINARY;
      SemaphoreSetCount( &ctx->tq_sem_ctx, 1 );
      ${/score/sem/req/seize-wait:/test-run}( &ctx->tq_sem_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-semaphore-wait-timed-ticks
    - role: validation
      uid: ../req/sys-lock-semaphore-clock-ticks
  - brief: |
      Validate the ${../if/sys-lock-semaphore-wait:/name} directive.
    code: |
      ctx->tq_sem_ctx.base.surrender = SemaphorePostBinary;
      ctx->tq_sem_ctx.base.wait = TQ_WAIT_FOREVER;
      ctx->tq_sem_ctx.variant = TQ_SEM_BINARY;
      SemaphoreSetCount( &ctx->tq_sem_ctx, 1 );
      ${/score/sem/req/seize-wait:/test-run}( &ctx->tq_sem_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-semaphore-wait
  - brief: |
      Validate the ${../if/sys-lock-semaphore-post:/name} directive.
    code: |
      ctx->tq_sem_ctx.base.surrender = SemaphorePost;
      ctx->tq_sem_ctx.base.wait = TQ_WAIT_FOREVER;
      ctx->tq_sem_ctx.variant = TQ_SEM_COUNTING_MODULO;
      SemaphoreSetCount( &ctx->tq_sem_ctx, 1 );
      ${/score/sem/req/surrender:/test-run}( &ctx->tq_sem_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-semaphore-post
  - brief: |
      Validate the ${../if/sys-lock-semaphore-post-binary:/name} directive.
    code: |
      ctx->tq_sem_ctx.base.surrender = SemaphorePostBinary;
      ctx->tq_sem_ctx.base.wait = TQ_WAIT_FOREVER;
      ctx->tq_sem_ctx.variant = TQ_SEM_BINARY;
      SemaphoreSetCount( &ctx->tq_sem_ctx, 1 );
      ${/score/sem/req/surrender:/test-run}( &ctx->tq_sem_ctx );
    links:
    - role: validation
      uid: ../req/sys-lock-semaphore-post-binary
  - brief: |
      Destroy the semaphore.
    code: |
      _Semaphore_Destroy( &sem );
    links: []
  links: []
test-brief: |
  Tests the `<sys/lock.h>` mutex and semaphore directives.
test-context:
- brief: |
    This member contains the thread queue test context.
  description: null
  member: |
    union {
      TQContext tq_ctx;
      TQMtxContext tq_mtx_ctx;
      TQSemContext tq_sem_ctx;
    }
test-context-support: null
test-description: null
test-header: null
test-includes:
- errno.h
- string.h
- sys/lock.h
test-local-includes:
- tr-mtx-seize-try.h
- tr-mtx-seize-wait.h
- tr-mtx-surrender.h
- tr-sem-seize-try.h
- tr-sem-seize-wait.h
- tr-sem-surrender.h
- tr-tq-timeout-priority-inherit.h
- tx-thread-queue.h
test-setup:
  brief: null
  code: |
    memset( ctx, 0, sizeof( *ctx ) );
    ctx->tq_ctx.enqueue_variant = TQ_ENQUEUE_BLOCKS;
    ctx->tq_ctx.discipline = TQ_PRIORITY;
    ctx->tq_ctx.deadlock = TQ_DEADLOCK_FATAL;
    ctx->tq_ctx.convert_status = TQConvertStatusPOSIX;
    ctx->tq_ctx.enqueue_prepare = TQEnqueuePrepareDefault;
    ctx->tq_ctx.enqueue_done = TQEnqueueDoneDefault;
    ctx->tq_ctx.get_owner = GetOwner;
    TQInitialize( &ctx->tq_ctx );
  description: null
test-stop: null
test-support: |
  static Status_Control MutexEnqueue( TQContext *ctx, TQWait wait )
  {
    const struct timespec abstime = {
      .tv_sec = INT64_MAX,
      .tv_nsec = 0
    };
    int                   eno;

    switch ( wait ) {
      case TQ_NO_WAIT:
        eno = _Mutex_Try_acquire( ctx->thread_queue_object );
        break;
      case TQ_WAIT_FOREVER:
        _Mutex_Acquire( ctx->thread_queue_object );
        eno = 0;
        break;
      case TQ_WAIT_TIMED:
        eno = _Mutex_Acquire_timed( ctx->thread_queue_object, &abstime );
        break;
      default:
        T_unreachable();
        break;
    }

    return STATUS_BUILD( 0, eno );
  }

  static Status_Control MutexSurrender( TQContext *ctx )
  {
    _Mutex_Release( ctx->thread_queue_object );

    return STATUS_SUCCESSFUL;
  }

  static rtems_tcb *GetOwner( TQContext *ctx )
  {
    const struct _Thread_queue_Queue *tq;

    tq = ctx->thread_queue_object;

    return tq->_owner;
  }

  static Status_Control RecursiveEnqueue( TQContext *ctx, TQWait wait )
  {
    const struct timespec abstime = {
      .tv_sec = INT64_MAX,
      .tv_nsec = 0
    };
    int                   eno;

    switch ( wait ) {
      case TQ_NO_WAIT:
        eno = _Mutex_recursive_Try_acquire( ctx->thread_queue_object );
        break;
      case TQ_WAIT_FOREVER:
        _Mutex_recursive_Acquire( ctx->thread_queue_object );
        eno = 0;
        break;
      case TQ_WAIT_TIMED:
        eno = _Mutex_recursive_Acquire_timed(
          ctx->thread_queue_object,
          &abstime
        );
        break;
      default:
        T_unreachable();
        break;
    }

    return STATUS_BUILD( 0, eno );
  }

  static Status_Control RecursiveSurrender( TQContext *ctx )
  {
    _Mutex_recursive_Release( ctx->thread_queue_object );

    return STATUS_SUCCESSFUL;
  }

  static Status_Control SemaphoreEnqueue( TQContext *ctx, TQWait wait )
  {
    int eno;

    switch ( wait ) {
      case TQ_NO_WAIT:
        eno = _Semaphore_Try_wait( ctx->thread_queue_object );
        break;
      case TQ_WAIT_FOREVER:
        _Semaphore_Wait( ctx->thread_queue_object );
        eno = 0;
        break;
      case TQ_WAIT_TIMED:
        eno = _Semaphore_Wait_timed_ticks( ctx->thread_queue_object, UINT32_MAX );
        break;
      default:
        T_unreachable();
        break;
    }

    return STATUS_BUILD( 0, eno );
  }

  static Status_Control SemaphorePost( TQContext *ctx )
  {
    _Semaphore_Post( ctx->thread_queue_object );

    return STATUS_SUCCESSFUL;
  }

  static Status_Control SemaphorePostBinary( TQContext *ctx )
  {
    _Semaphore_Post_binary( ctx->thread_queue_object );

    return STATUS_SUCCESSFUL;
  }

  static uint32_t SemaphoreGetCount( TQSemContext *ctx )
  {
    const struct _Semaphore_Control *sem;

    sem = ctx->base.thread_queue_object;

    return sem->_count;
  }

  static void SemaphoreSetCount( TQSemContext *ctx, uint32_t count )
  {
    struct _Semaphore_Control *sem;

    sem = ctx->base.thread_queue_object;
    sem->_count = count;
  }
test-target: testsuites/validation/tc-sys-lock.c
test-teardown:
  brief: null
  code: |
    TQDestroy( &ctx->tq_ctx );
    RestoreRunnerPriority();
  description: null
type: test-case
